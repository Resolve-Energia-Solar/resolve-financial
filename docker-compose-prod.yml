version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    command: sh -c "python3 manage.py collectstatic --noinput && gunicorn -c ./configs/configs.py crm_resolve.wsgi"
    ports:
      - "8060:8060"
